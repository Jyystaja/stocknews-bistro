// GPT Engineer Core Functionality
const initGPTEngineer = () => {
  // Core functionality without the select button
  console.log("GPT Engineer initialized without select button");
};

// Initialize on load
window.addEventListener('load', initGPTEngineer);

export default initGPTEngineer;